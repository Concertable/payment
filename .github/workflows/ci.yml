name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  merge_group:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read
  packages: read

env:
  GITHUB_PACKAGES_TOKEN: ${{ secrets.CONCERTABLE_PACKAGES_TOKEN || secrets.GITHUB_TOKEN }}
  DOTNET_NOLOGO: true

jobs:
  build-test-pack:
    name: Build, unit test, and pack
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x
      - name: Restore repository-local solution
        run: dotnet restore Concertable.Payment.slnx
      - name: Build repository-local solution
        run: dotnet build Concertable.Payment.slnx --configuration Release --no-restore
      - name: Run unit tests
        run: dotnet test tests/Concertable.Payment.UnitTests/Concertable.Payment.UnitTests.csproj --configuration Release --no-restore
      - name: Run architecture tests
        run: dotnet test tests/Concertable.Payment.ArchitectureTests/Concertable.Payment.ArchitectureTests.csproj --configuration Release --no-restore
      - name: Run startup and composition tests
        run: dotnet test tests/Concertable.Payment.StartupTests/Concertable.Payment.StartupTests.csproj --configuration Release --no-restore
      - name: Run E2E admin surface tests
        run: dotnet test tests/Concertable.Payment.E2EAdmin.IntegrationTests/Concertable.Payment.E2EAdmin.IntegrationTests.csproj --configuration Release --no-restore
      - name: Pack Payment-owned packages
        run: |
          dotnet pack src/Concertable.Payment.Contracts/Concertable.Payment.Contracts.csproj --configuration Release --output artifacts/packages
          dotnet pack src/Concertable.Payment.Client/Concertable.Payment.Client.csproj --configuration Release --output artifacts/packages
          dotnet pack src/Concertable.Payment.Hosting/Concertable.Payment.Hosting.csproj --configuration Release --output artifacts/packages
          dotnet pack src/Concertable.Payment.TestKit/Concertable.Payment.TestKit.csproj --configuration Release --output artifacts/packages

  integration-tests:
    name: Integration tests
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x
      - name: Restore integration tests
        run: dotnet restore tests/Concertable.Payment.IntegrationTests/Concertable.Payment.IntegrationTests.csproj
      - name: Run Docker-backed integration tests
        run: dotnet test tests/Concertable.Payment.IntegrationTests/Concertable.Payment.IntegrationTests.csproj --configuration Release --no-restore

  ci-complete:
    name: CI complete
    if: ${{ always() }}
    needs:
      - build-test-pack
      - integration-tests
    runs-on: ubuntu-latest
    steps:
      - name: Require successful Payment checks
        env:
          BUILD_TEST_PACK_RESULT: ${{ needs.build-test-pack.result }}
          INTEGRATION_TESTS_RESULT: ${{ needs.integration-tests.result }}
        run: |
          test "$BUILD_TEST_PACK_RESULT" = "success"
          test "$INTEGRATION_TESTS_RESULT" = "success"
